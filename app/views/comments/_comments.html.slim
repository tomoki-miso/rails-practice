h4 コメント

#comments_list.index-items
  - unless commentable.comments.exists?(reply_comment_id: nil)
    p id="no_comments_found" コメントはありません。

  - commentable.comments.top_level.includes(:user, replies: :user).each do |comment|
    = render comment
